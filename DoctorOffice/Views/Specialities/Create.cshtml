@{
  Layout = "_Layout";
}

@model DoctorOffice.Models.Speciality


<h4>Add a new speciality</h4>
@using (Html.BeginForm())
{
  @Html.LabelFor(model => model.Name)
  @Html.TextBoxFor(model => model.Name)

  <input type="submit" value="Add new speciality" />
}
<p>@Html.ActionLink("Show all specialities", "Index")</p>